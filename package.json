{
  "name": "nearmeu-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"mocha test/firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "mocha": "^10.8.2",
    "firebase-tools": "^13.35.1"
  }
}
